[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-email-router"
version = "1.0.0"
description = "AI-powered email classification and routing engine"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.11"
authors = [{name = "AI Email Router"}]

[project.urls]
Homepage = "https://github.com/23e4rtyuyf/turbo-octo-dollop"
